<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SurveyMapper">
    <select id="getSurveys" resultType="java.util.HashMap">
        SELECT * FROM SURVEY
    </select>

    <select id="selectSurvey" resultType="com.survey_in.entity.Survey">
        SELECT * from SURVEY
        WHERE SURVEY_ID = #{survey_id}
    </select>

    <insert id="insertSurvey" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO SURVEY (MEMBER_ID, TITLE, CATEGORY, CAPACITY, POINT, QUESTION_CNT)
        VALUES(#{member_id}, #{title}, #{category}, #{capacity}, #{point}, #{questionCnt})
    </insert>

    <delete id="deleteSurvey">
        DELETE FROM SURVEY
        WHERE SURVEY_ID = #{survey_id}
    </delete>

    <select id="selectMemberSurveys" resultType="com.survey_in.entity.Survey">
        SELECT * from SURVEY
        WHERE MEMBER_ID = #{member_id}
    </select>


<!-- ####### search ####### -->
    <select id="getCntSearchSurvey" resultType="int">
        SELECT COUNT(*)
        <include refid="searchBySurvey"/>
    </select>

    <select id="searchSurvey" resultType="com.survey_in.entity.Survey">
        SELECT *
        <include refid="searchBySurvey"/>
        <choose>
            <when test="filter.ord == 'latest'"> ORDER BY created_at DESC </when>
            <when test="filter.ord == 'point'">  ORDER BY point DESC      </when>
        </choose>
        LIMIT #{paging.page.surveyPerPage}
        OFFSET #{paging.page.surveyStart}
    </select>

    <sql id="searchBySurvey">
        FROM survey
        WHERE title LIKE '%'||#{keyword}||'%'
        <if test="filter.cat != 'all'">    AND category = #{filter.cat}      </if>
        <if test="filter.age != 'all'">    AND age_limit = #{filter.age}     </if>
        <if test="filter.gen != 'all'">    AND gender_limit = #{filter.gen}  </if>
    </sql>
</mapper>